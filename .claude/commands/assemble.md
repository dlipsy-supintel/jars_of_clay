Assemble the final deployment guide from modular doc sections. Execute:

```bash
bash scripts/assemble-guide.sh
```

Report the number of sections assembled and total line count. If the line count exceeds 1,200 lines, flag sections that could be trimmed.
